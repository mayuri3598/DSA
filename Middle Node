const LinkedListNode = class {
    constructor(nodeData) {
        this.data = nodeData;
        this.next = null;
    }
};

var middleNode = function(head) {
     var count=0;
    var midd=head;
    while(head!==null){
        if((count%2)==1)
        midd=midd.next;
        count++;
        head=head.next;
    }
    if(midd!==head){
        //console.log(midd.data)
    return(midd.data)  
    }
};