const LinkedListNode = class {
    constructor(nodeData) {
        this.data = nodeData;
        this.next = null;
    }
};
// Complete the function below
function deleteNode(node){
//console.log(node);
         let res=node .next;
    node.data=res.data;
    node.next=res.next;
    res=null;
}